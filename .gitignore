# === Godot Engine .gitignore ===
# Этот файл говорит системе контроля версий (например, Git),
# какие файлы и папки нужно ИГНОРИРОВАТЬ.

# Главный "мусор" - папка с временными файлами и импортированным кэшем.
# Ее НИКОГДА нельзя добавлять в проект. Движок создает ее сам.
.godot/

# Файлы, связанные с Mono/C# (мы используем GDScript, но на всякий случай)
.mono/
project.godot.mono

# Файлы кеша и метаданных редактора
.godot-defs/
.godot-launch.json
.vscode/

# Импортированные файлы и их метаданные.
# Хранить нужно только исходники (например, .png), а не их .import-версии.
*.import

# Файлы переводов (генерируются автоматически)
*.translation
*.remap
*.rids

# Конфигурация экспорта. Может содержать личные данные/пути.
# Лучше настраивать ее на каждом ПК отдельно.
export_presets.cfg

# Резервные копии, создаваемые некоторыми редакторами.
*~
*.bak
*.swp